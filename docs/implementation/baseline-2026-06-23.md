# Baseline Capture — 2026-06-23

## Rama: feat/claudecode-visionflow-e2e-infra (base: development)

## Resultados

| Check | Resultado |
|---|---|
| lint | 0 errors, 1 warning (unused eslint-disable en layout.tsx) |
| typecheck | PASS (0 errores) |
| test | 2 FAIL / 70 PASS — workspace-governance.test.ts busca migración SQLite legacy (20260621120000_add_workspace_governance) que no existe; cubiertos con continue-on-error en CI actual |
| build | PASS — standalone output OK |

## Hallazgos pre-Fase 0

- `DATABASE_URL` en CI apunta a `file:./dev.db` (SQLite) — contradicción con schema PostgreSQL
- `continue-on-error: true` en step `Test` del CI
- `access: "public"` en upload de blobs (`src/app/api/vision/maps/[id]/exports/upload/route.ts:110`)
- Fallback `NEXT_PUBLIC_OPENROUTER_API_KEY` en `src/lib/llm-client.ts:15`
- Sin CSP en `next.config.ts`
- 2 tests fallan por referencia a migración SQLite legacy (F0-T5 la marcará como histórico)

## Stack confirmado

- Next.js (standalone) + React + Tailwind + Prisma + PostgreSQL (Neon)
- Vitest + bun
- Vercel Blob para exports
- OpenRouter como LLM provider

---

## Informe de cierre — Fase 0 (2026-06-23)

### Gate de salida: SUPERADO

| Criterio | Estado |
|---|---|
| CI reproducible en verde sobre PostgreSQL efímero | ✅ ci.yml + todos los promote workflows migrados a postgres:16 |
| Sin continue-on-error en gates promocionables | ✅ Eliminado de ci.yml, validate-agent-branch.yml, promote-development-to-staging.yml |
| No existe export con access público | ✅ `access: "private"` + proxy server-side via `get()` con `{ access: "private" }` |
| Sin secretos NEXT_PUBLIC_*; test de detección en verde | ✅ Fallback eliminado en llm-client.ts; security-hardening.test.ts en verde |
| Documentación y código coinciden en stack | ✅ Banners históricos en DESIGN.md + REQUIREMENTS.md; decision-register actualizado |
| Guard de divergencia en CI | ✅ scripts/check-stack-divergence.sh integrado en ci.yml |
| CSP report-only añadida | ✅ Content-Security-Policy-Report-Only en next.config.ts |
| Suite de tests en verde | ✅ 12 ficheros passed, 2 tests skipped (legacy SQLite obsoleto, marcado con describe.skip) |
| Build standalone | ✅ |

### Archivos modificados (14)

- `.github/workflows/ci.yml` — PostgreSQL 16 service, stack divergence check, sin continue-on-error
- `.github/workflows/promote-*.yml` (×3) — mismo tratamiento + `migrate deploy`
- `.github/workflows/validate-agent-branch.yml` — ídem
- `next.config.ts` — CSP report-only
- `src/app/api/vision/maps/[id]/exports/upload/route.ts` — `access: "private"`
- `src/app/api/vision/maps/[id]/exports/[exportId]/download/route.ts` — proxy privado via `get()`
- `src/components/vision/ExportHistory.tsx` — URL de copia apunta a ruta autenticada
- `src/lib/llm-client.ts` — eliminado fallback `NEXT_PUBLIC_OPENROUTER_API_KEY`
- `src/lib/security-hardening.test.ts` — nuevo; detección de secretos públicos y exports
- `src/lib/workspace-governance.test.ts` — legacy SQLite suite marcada como `describe.skip`
- `docs/implementation/decision-register.md` — DEC-OSS-001 y DEC-DB-001 resueltos
- `docs/specs/anclora-visionflow/DESIGN.md` — banner histórico SQLite/z-ai-web-dev-sdk
- `docs/specs/anclora-visionflow/REQUIREMENTS.md` — ídem
- `scripts/check-stack-divergence.sh` — nuevo; guard de divergencia doc/código

### Riesgos pendientes para Fase 1

1. **2 tests skipped** (`workspace-governance.test.ts`) — requieren reescritura para PostgreSQL en Fase 1 o como tarea independiente
2. **Export TTL hardcodeado en 24h** — se sustituirá por campo `expiresAt` en `MapExport` (Fase 2, modelo `EvidenceReference`)
3. **next-auth no cableado** — DEC-AUTH-001 pendiente, crítico para Fase 1 (F1-T1)
4. **Rate limit en memoria** — F1-T7 lo reemplazará por Valkey/Redis

Sin commits ni push. Árbol listo para revisión humana.
